<p id="notice"><%= notice %></p>

<h1>Client Area Orders</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @client_area_orders.each do |client_area_order| %>
      <tr>
        <td><%= link_to 'Show', client_area_order %></td>
        <td><%= link_to 'Edit', edit_client_area_order_path(client_area_order) %></td>
        <td><%= link_to 'Destroy', client_area_order, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Client Area Order', new_client_area_order_path %>
