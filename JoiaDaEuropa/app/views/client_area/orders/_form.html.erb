<%= form_for(client_area_order) do |f| %>
    <%= f.text_field :date %>
  <% if client_area_order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(client_area_order.errors.count, "error") %> prohibited this client_area_order from being saved:</h2>

      <ul>
      <% client_area_order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
